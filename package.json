{
  "name": "linear-zapier",
  "version": "1.0.1",
  "description": "Linear Zapier integration",
  "main": "index.js",
  "scripts": {
    "zapier-build": "tsc",
    "zapier-dev": "tsc --watch",
    "zapier-push": "npm run zapier-build && zapier push",
    "prepare": "npm run zapier-build",
    "pretest": "npm run zapier-build",
    "test": "mocha --recursive lib/test"
  },
  "engines": {
    "node": "8.10.0",
    "npm": ">=5.6.0"
  },
  "dependencies": {
    "zapier-platform-core": "8.2.1"
  },
  "devDependencies": {
    "@types/node": "^12.11.1",
    "mocha": "^5.2.0",
    "should": "^13.2.0",
    "typescript": "^3.6.4"
  },
  "private": true
}
